name : My first worflow

on : push

jobs:
    first_job:
       runs-on: ubuntu-latest
       steps:
       - name: Welcome message 
         run: echo "My first Github action Job"

       - name: List file
         run: ls 

       - name: rRead file
         run: cat README.md
